type: object
required:
  - id
  - type
  - name
  - owner
model:searchProperties:
  - name
db:options:
  timestamps: true
properties:
  id:
    $ref: './../$defs.yaml#/NGSIentityFormat'
    description: 'Unique identifier of the entity'
    default: EmergencyEvent:00000
    ui:hide:
      table: true
      form: true
  type:
    type: string
    enum:
      - EmergencyEvent
    default: EmergencyEvent
    ui:hide:
      form: true
      table: true
  name:
    type: string
    description: 'The name of this item.'
    ui:formItemType: text
    ui:formItemGridClass: 'col-md-6'
  description:
    type: string
    description: 'A description of this item'
    ui:formItemType: textarea
  owner:
    $ref: './../$defs.yaml#/owner'
  createdAt:
    type: string
    format: "date-time"
    db:skipInSchemaDefinition: true
  updatedAt:
    type: string
    format: "date-time"
    db:skipInSchemaDefinition: true
